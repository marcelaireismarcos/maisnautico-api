# Configuração do Render.com — deploy automático
services:
  - type: web
    name: maisflamengo-api
    env: node
    region: oregon
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
